"""

Python 的小括號 ()、中括號 [] 與大括號 {} 分別用來代表元組 (Tuple)、列表 (List) 以及字典 (Dict) 和集合 (Set)。

小括號 ( )
元組 (Tuple)：建立不可變動的資料序列，例如 (1, 2, 3)。
函數呼叫：用來傳遞參數或執行函數，例如 print("Hello")。
運算優先權：用來改變數學運算的先後順序，例如 (1 + 2) * 3。

中括號 [ ]
列表 (List)：建立可變動的資料序列，例如 [1, 2, 3]。
索引與切片 (Index/Slice)：用來讀取或取得序列與字串中的元素，例如 my_list[0] 或 text[1:3]。

大括號 { }
字典 (Dict)：用來建立鍵值對 (Key-Value) 的資料，例如 {'name': 'John', 'age': 20}。
集合 (Set)：用來建立不重複元素的集合，例如 {1, 2, 3}。

"""
